apiVersion: v1
kind: Service
metadata:
  name: node-k8s
  labels:
    app: node-k8s
spec:
  selector:
    app: node-k8s
  ports:
    - name: "http"
      port: 3000
      targetPort: 3000
      nodePort: 30000  # NodePort must be between 30000-32767
  type: NodePort